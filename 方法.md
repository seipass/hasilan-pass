# Hasilan Pass Windows署名付きRelease手順

この手順は、Hasilan PassのWindows Authenticode署名付きReleaseを作成し、GitHub
Releaseとして公開するためのものです。現在のRelease Workflowは、タグを付けたコミットの
CI成功、Windows署名、Android署名、SBOM、チェックサム、Provenance/Attestationを確認した
あとにDraft Releaseを作成します。Draftを公開する操作だけは手動です。

対象Workflow: [`.github/workflows/release.yml`](.github/workflows/release.yml)

## 0. 重要な注意

- PFX、秘密キー、PFXパスワード、Base64化した証明書をコミットしない。
- これらの値をチャット、Issue、Pull Request、Webhook、ログに貼り付けない。
- GitHubにはEnvironment Secretとして直接登録する。
- 署名証明書またはパスワードが漏れた場合は、証明書を失効・再発行し、GitHubのSecretを更新する。
- このWorkflowはWindowsの署名用PFXを要求する。秘密キーをエクスポートできないUSBトークンやHSMの証明書は、そのままでは使えない。

## 1. Windowsコード署名証明書を用意する

### 1.1 必要な条件

公開配布用には、信頼された認証局（CA）が発行したWindows Authenticodeコード署名証明書を使う。
証明書は次の条件を満たす必要がある。

- 署名用途の証明書である。
- 秘密キーを含むPFX（`.pfx`または`.p12`）としてエクスポートできる。
- 署名時点で有効期限内である。
- PFX内に秘密キー付きの署名証明書が1つだけある。

自己署名証明書は開発・テスト用であり、公開配布ではWindowsの信頼警告が発生する。証明書の選択については[Microsoftのコード署名案内](https://learn.microsoft.com/en-us/windows/apps/package-and-deploy/code-signing-options)を参照する。

すでに条件を満たすPFXを持っている場合は、1.2を飛ばして2へ進む。

### 1.2 Windowsの証明書ストアからPFXをエクスポートする

1. 署名証明書をインストールしたWindows PCで `certmgr.msc` を起動する。
2. **個人** → **証明書**を開く。
3. コード署名証明書を右クリックする。
4. **すべてのタスク** → **エクスポート**を選ぶ。
5. **はい、秘密キーをエクスポートします**を選ぶ。
6. ファイル形式に **Personal Information Exchange（PFX）** を選ぶ。
7. 必要なら証明書チェーンを含める。ただし、別の秘密キー付き証明書は含めない。
8. 強いパスワードを設定し、`hasilan-pass-code-signing.pfx` として保存する。

PFXエクスポートはPowerShellでも実行できる。証明書が現在のユーザーストアにあり、秘密キーがエクスポート可能な場合の例:

```powershell
$thumbprint = Read-Host "証明書のThumbprint"
$pfxPassword = Read-Host "PFX password" -AsSecureString

Get-ChildItem "Cert:\CurrentUser\My\$thumbprint" |
  Export-PfxCertificate `
    -FilePath .\hasilan-pass-code-signing.pfx `
    -Password $pfxPassword
```

詳細は[MicrosoftのExport-PfxCertificateリファレンス](https://learn.microsoft.com/en-us/powershell/module/pki/export-pfxcertificate)を参照する。

## 2. PFXをBase64に変換する

GitHub SecretにはPFXバイナリではなく、PFX全体をBase64化した1行の文字列を登録する。

PowerShellで、PFXを保存したディレクトリから実行する。

```powershell
$pfx = Join-Path $PWD "hasilan-pass-code-signing.pfx"
$out = Join-Path $PWD "WINDOWS_CERTIFICATE.base64.txt"

[IO.File]::WriteAllText(
  $out,
  [Convert]::ToBase64String([IO.File]::ReadAllBytes($pfx)),
  [Text.Encoding]::ASCII
)
```

`WINDOWS_CERTIFICATE.base64.txt`をメモ帳で開き、内容をすべてコピーする。内容を端末ログやチャットに出力しない。

### 2.1 推奨: 登録スクリプトを使う

手動でBase64文字列をコピーせず、リポジトリのスクリプトからGitHubへ直接登録できる。スクリプトはPFXをメモリ上でBase64化し、GitHub CLIの標準入力へ渡す。PFXパスワードはコマンドライン引数やファイルには保存せず、非表示入力する。

前提:

- GitHub CLI（`gh`）をインストールし、`gh auth login`を完了している。
- `release` Environmentが作成済みである。
- CA発行の署名用PFXと、CA指定のHTTPS RFC 3161 URLを持っている。

まず書き込みを行わない検証を実行する。

```powershell
node scripts/configure-windows-release-signing.mjs `
  --pfx "C:\secure\hasilan-pass-code-signing.pfx" `
  --timestamp-url "https://<CAのRFC3161エンドポイント>" `
  --dry-run
```

問題がなければ、`--dry-run`を外して実行する。リポジトリの`origin`から`owner/name`を自動検出する。自動検出できない場合は`--repo seipass/hasilan-pass`を追加する。

```powershell
node scripts/configure-windows-release-signing.mjs `
  --pfx "C:\secure\hasilan-pass-code-signing.pfx" `
  --timestamp-url "https://<CAのRFC3161エンドポイント>"
```

スクリプトは次の3つを`release` Environmentへ登録する。

- `WINDOWS_CERTIFICATE`
- `WINDOWS_CERTIFICATE_PASSWORD`
- `WINDOWS_TIMESTAMP_URL`

登録後は7へ進み、Release Workflowを再実行する。スクリプトは信頼されたCA証明書の発行や、秘密キーをエクスポートできないUSBトークン/HSMからのPFX作成は行わない。

## 3. 無料の署名なしWindows Releaseに切り替える場合

PFXを用意せずにGitHub Releaseを作ることはできるが、Windows版は未署名になる。SmartScreenや企業ポリシーによって警告・ブロックされる可能性があるため、一般利用者向けの信頼済み配布ではない。

現在のWorkflowは、誤って未署名Releaseを作らないよう、明示的なオプトインがない限りタグReleaseを停止する。

タグReleaseで一時的に許可する場合:

1. [Release Environment設定](https://github.com/seipass/hasilan-pass/settings/environments)を開く。
2. `release` Environmentの**Environment variables**に次を追加する。

| 名前 | 値 |
| --- | --- |
| `ALLOW_UNSIGNED_WINDOWS_RELEASE` | `true` |

3. Windowsの署名Secretは登録しないままでよい。
4. Workflowを含む変更を`main`に反映してから、新しい`v*`タグでRelease候補を作る。

タグReleaseでは、Windows成果物のメタデータが`nativeCodeSigning: unsigned-smoke`になり、`windows-signing-warning.txt`が添付される。Android署名、SBOM、チェックサム、Provenance、Attestation、CI成功条件は維持される。

手動の`workflow_dispatch`では、入力項目**Allow an explicitly disclosed unsigned Windows package**を有効にすると、同じ署名なしWindowsの確認用成果物を作れる。ただし手動実行はDraft Releaseを作成しない。

## 4. タイムスタンプURLを確認する

証明書発行元（CA）の管理画面または発行案内から、公開されたRFC 3161タイムスタンプURLを確認する。

このWorkflowでは、値が次の条件を満たさないと失敗する。

- 絶対URLである。
- `https://`で始まる。
- 署名時に利用できるRFC 3161タイムスタンプサービスである。

`WINDOWS_TIMESTAMP_URL`は秘密情報ではないが、証明書発行元が指定するURLをそのまま使う。`http://`しか案内されていない場合は、CAにHTTPS対応のRFC 3161エンドポイントを確認する。Windows署名のRFC 3161設定については[Microsoftのタイムスタンプ案内](https://learn.microsoft.com/en-us/windows/win32/seccrypto/time-stamping-authenticode-signatures)を参照する。

## 5. GitHubの `release` Environmentに登録する

管理者権限で[Hasilan PassのEnvironment設定](https://github.com/seipass/hasilan-pass/settings/environments)を開く。

1. `release` Environmentを開く。
2. `release`がなければ **New environment** で、名前を正確に `release` として作成する。
3. **Environment secrets** に次の2つを追加する。

| 名前 | 登録する値 |
| --- | --- |
| `WINDOWS_CERTIFICATE` | `WINDOWS_CERTIFICATE.base64.txt`の内容全体 |
| `WINDOWS_CERTIFICATE_PASSWORD` | PFXをエクスポートしたときに設定したパスワード |

4. **Environment variables** に次を追加する。

| 名前 | 登録する値 |
| --- | --- |
| `WINDOWS_TIMESTAMP_URL` | CAのHTTPS RFC 3161タイムスタンプURL |

名前は大文字・アンダースコアを含めて完全一致させる。`WINDOWS_TIMESTAMP_URL`はSecretではなくEnvironment variableに登録する。

WorkflowのWindowsおよびAndroidの署名Jobは `environment: release` を指定しているため、値はこのEnvironmentに登録する。EnvironmentのRequired reviewersを設定している場合、承認されるまでJobはSecretにアクセスできない。[GitHub公式: Environments](https://docs.github.com/en/actions/how-tos/deploy/configure-and-manage-deployments/manage-environments)

## 6. 登録前チェックリスト

- [ ] `WINDOWS_CERTIFICATE`が空でない。
- [ ] Base64文字列の途中に欠落や余計な引用符がない。
- [ ] `WINDOWS_CERTIFICATE_PASSWORD`がPFX作成時のパスワードと一致する。
- [ ] PFXに秘密キー付き証明書が1つある。
- [ ] 証明書の有効期限が切れていない。
- [ ] `WINDOWS_TIMESTAMP_URL`がCA指定の`https://` URLである。
- [ ] `release` EnvironmentのRequired reviewersがある場合、承認できる人がいる。
- [ ] PFXとBase64一時ファイルをGit管理対象外にしている。

## 7. 既存の `v0.1.0` Release候補を再実行する

今回失敗したRunは[Release candidate run 32453849297](https://github.com/seipass/hasilan-pass/actions/runs/32453849297)である。

### GitHub画面から再実行

1. Runページを開く。
2. 右上の **Re-run jobs** を押す。
3. **Re-run all jobs** を選ぶ。
4. 必要なEnvironment承認が表示されたら承認する。

**Re-run all jobs**を選ぶ。Windowsだけを再実行すると、前回失敗でSkipされたSBOM・チェックサム・Draft作成Jobまで確実に再実行されない場合がある。

### GitHub CLIから再実行

```bash
gh run rerun 32453849297 --repo seipass/hasilan-pass
gh run watch 32453849297 --repo seipass/hasilan-pass --interval 20 --exit-status
```

再実行は元のWorkflowのコミットSHAとタグを使うため、`v0.1.0`タグを作り直す必要はない。[GitHub公式: Workflowの再実行](https://docs.github.com/en/actions/how-tos/manage-workflow-runs/re-run-workflows-and-jobs)および[GitHub CLI: `gh run rerun`](https://cli.github.com/manual/gh_run_rerun)

## 8. Draft Releaseを確認して公開する

Workflowがすべて成功すると、次の処理が行われる。

- Windows NSIS/MSIの署名検証
- Android APK/AABの署名検証
- SBOM生成
- SHA-256チェックサム生成
- Provenance/Attestation生成
- `v0.1.0`のDraft Release作成または更新

[Releasesページ](https://github.com/seipass/hasilan-pass/releases)を開き、次を確認する。

1. Windows、Android、Linux、Web、拡張機能のアセットが揃っている。
2. `SHA256SUMS`、SBOM、Attestationファイルが含まれている。
3. バージョンとタグが一致している。
4. 必要なリリースノートを確認する。
5. 問題がなければ **Publish release** を押す。

CLIでDraftの存在を確認する場合:

```bash
gh release view v0.1.0 --repo seipass/hasilan-pass
```

## 9. よくある失敗と対処

### `a tagged release requires Windows Authenticode credentials...`

3つの値のいずれかが未登録、または別の場所に登録されている。`release` Environmentの名前とSecret/variable名を確認する。

### `Windows signing credentials are only partially configured`

3つのうち一部だけが設定されている。3つすべてを設定するか、値をすべて削除してから再登録する。

### `WINDOWS_TIMESTAMP_URL must be an absolute HTTPS URL`

URLが`https://`で始まっていない、またはURL全体が壊れている。CAに正しいRFC 3161 URLを確認する。

### `the PFX must import exactly one certificate with a private key`

秘密キーなしの`.cer`を登録している、またはPFX内に秘密キー付き証明書が複数ある。コード署名証明書1つを、**秘密キーを含めて**PFXに再エクスポートする。

### Authenticode statusが`Unknown`または`NotTrusted`

公開信頼されていない証明書、証明書チェーン不足、または期限切れの可能性がある。CA発行のコード署名証明書とチェーンを確認する。

### 署名設定後もWorkflowが待機中になる

`release` EnvironmentのRequired reviewers待ちの可能性がある。Workflow画面でDeploymentを開き、必要な承認を行う。

## 10. 作業後の片付け

成功を確認したら、ローカルの`WINDOWS_CERTIFICATE.base64.txt`を削除し、PFXはパスワードマネージャーまたは暗号化ストレージで保管する。PFXを失うと同じ証明書での再署名ができないため、バックアップは安全に保管する。
